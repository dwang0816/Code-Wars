Solution:

function anagrams(word, words) {
  let character = word.split('').sort().join('')
  let matches = []
  for(i = 0;i<words.length;i++){
    if(words[i].split('').sort().join('')===character){
    matches.push(words[i])
    }
  }
  return matches
}
